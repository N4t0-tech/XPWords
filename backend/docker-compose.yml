services:
  postgres:
    image: postgres:16-alpine
    container_name: xpwords-db
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: xpwords
      POSTGRES_USER: xpwords
      POSTGRES_PASSWORD: xpwords_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
